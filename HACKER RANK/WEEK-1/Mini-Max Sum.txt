 long total_sum = 0;
    long min_value = LONG_MAX;
    long max_value = LONG_MIN;
     for (int i = 0; i < arr_count; i++) {
        total_sum += arr[i];
        if (arr[i] < min_value) {
            min_value = arr[i];
        }
        if (arr[i] > max_value) {
            max_value = arr[i];
        }
    }
    long min_sum = total_sum - max_value; 
    long max_sum = total_sum - min_value; 
    printf("%ld %ld\n", min_sum, max_sum);